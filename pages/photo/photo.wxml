<view class="container">
  <swiper class="home-swiper" indicator-dots="true" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
    <block wx:for-items="{{lunboData}}" wx:key="{{lunboData.lunbo}}">
      <swiper-item>
        <image src="{{item.imgurl}}" class="slide-image" style="width: {{imagewidth}}px; height: {{imageheight}}px;" bindload="imageLoad"></image>
      </swiper-item>
    </block>
  </swiper>
</view>